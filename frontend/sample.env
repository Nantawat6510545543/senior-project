# ==========================================
# Frontend Environment Variables (Vite)
# Instructions: Copy this file, rename it to '.env', 
# and update the URLs if deploying to production.
# ==========================================

# The URL of your running backend API (FastAPI)
# Note: For local development, keep this as localhost:8000
VITE_BACKEND_URL=http://localhost:8000
